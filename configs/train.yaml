seed: 42
data:
  images: data/raw/coco/train2014
  annotations: data/raw/coco/annotations/captions_train2014.json
model:
  embedding_dim: 512
  hidden_dim: 512
  train_backbone: false
training:
  epochs: 10
  batch_size: 32
  learning_rate: 0.0003
